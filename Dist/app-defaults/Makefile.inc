CURDIR = lib/app-defaults

SUBDIRS =

USER_INSTALL_SUBDIRS = $(SUBDIRS)
DEVKIT_INSTALL_SUBDIRS = $(SUBDIRS)

USER_INSTALL_FILES = \
$(PROJECT_NAME).* \
$(PROJECT_NAME) \
*.sql \
Xdefaults \
Product.defs

